# ===== RUNTIME CONFIGS (never commit — may contain phone numbers, LIDs, keys) =====
# Hanya file config AKTIF yang di-ignore. Semua *.example TETAP di-commit.
config.json
config.yaml
config.yml
voice_context_config.json

# ===== RUNTIME ARTIFACTS (generated saat jalan, jangan commit) =====
*.log
*.pid
.env
smoke/
runtime/
backup-*/

# ===== PYTHON =====
__pycache__/
*.pyc
.venv/
.pytest_cache/

# ===== BUILD ARTIFACTS (binary hasil go build — source TIDAK pernah di-ignore) =====
/bridge/meowcaller-poc
