# Secrets & runtime (JANGAN commit)
config.json
config.yaml
*.log
*.pid
.env
*.bak-*
__pycache__/
*.pyc

# Build artifacts
meowcaller-poc
bin/

# ===== LOCAL RUNTIME CONFIGS (never commit) =====
# May contain real phone numbers, WhatsApp LIDs, or server paths
config.yaml
config.yml
config.json
voice_context_config.json
